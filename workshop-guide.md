# GitHub Copilot ワークショップ アジェンダ（2026-08-13）

## 1. ワークショップの概要

### 目的

GitHub Copilot を単なるコード補完ツールではなく、既存システムの理解、設計、実装、レビュー、テスト、リスク管理までを支援する開発エージェントとして体験いただく。

### 所要時間・形式

- 全体: 2.5 時間（150 分）
- 形式: 講師デモを中心に、短いガイド付き体験と対話を組み合わせる

### 想定参加者

- 管理職を含む、GitHub Copilot をこれまで直接利用していない方
- ソフトウェア開発への関与や技術知識には幅がある前提
- コードの詳細な読み書きよりも、GitHub Copilot で何が変わるか、どこを人が判断すべきかに関心がある方

### 参加者に持ち帰っていただきたいこと

1. Agentic Development と従来の AI コード補完の違い
2. GitHub Copilot に既存プロジェクトの概要を説明させる体験
3. 既存コードから設計書やワイヤーフレームが生成される流れ
4. 実装、コードレビュー、テストをエージェントがどのように支援するか
5. AI が生成した成果物をそのまま受け入れず、セキュリティや変更リスクを確認する視点

## 2. 全体アジェンダ

| 時間 | セッション | 形式 | 主な成果物・体験 |
|---:|---|---|---|
| 0:00-0:10 | オープニング | 説明 | 本日のゴール、前提、デモ対象の共有 |
| 0:10-0:40 | Part 1: Agentic Development を体験する | ライブデモ | 自然言語の要求から動く成果物を作る体験 |
| 0:40-0:50 | 振り返りと Part 2 への接続 | 対話 | エージェントに任せる範囲と人が判断する範囲の整理 |
| 0:50-1:20 | Part 2-1: 既存システムを理解する | デモ／ガイド付き体験 | システム概要、構成図、主要処理、依存関係 |
| 1:20-1:50 | Part 2-2: ドキュメントと変更設計を作る | ガイド付き体験 | 現行仕様書、変更設計書、ワイヤーフレーム |
| 1:50-2:15 | Part 2-3: 変更を実装し、レビューとテストを行う | 講師デモ／対話 | 実装差分、レビュー結果、テストコード、テスト結果 |
| 2:15-2:25 | Part 2-4: リスクとセキュリティを確認する | デモ／対話 | リスク一覧、セキュリティ観点、修正案 |
| 2:25-2:30 | クロージング | 振り返り | AI Native 開発ライフサイクルの持ち帰り事項 |

合計: 150 分

## 3. Part 1: Agentic Development を体験する（30 分）

### 狙い

冒頭で「ここまでできるのか」という驚きを作り、GitHub Copilot がアプリケーションを生成し、計画し、実装し、結果を確認する様子を短時間で体験いただく。

### 体験の流れ

1. GitHub Copilot の画面と、今回利用するチャット操作を簡潔に説明する。
2. 身近で理解しやすい業務要求を自然言語で提示する。

### GitHub Copilot の基本機能
- プロンプト実行
- モデルの選択
- セッション
- パーミッション

### アプリケーションの生成

#### 初期作成

  **プロンプト例**

  あくまで一例です。皆様の業務に近い内容に寄せていただいて構いません。

  最初に生成するときは、ハイパフォーマンスのモデルを選択してください。例： GPT-5.6 Sol, Claude Opus 5, など

  ```text
  ブラウザで動くダッシュボードを vibe coding で作成します。
  社員の勤怠状況を週次で表示し、稼働が過多になっている社員をすぐに特定できるようにします。
  社員データは仮の名前で表示して20人ほどにしてください。
  ```

Learn: 指示が曖昧であっても、エージェントが要件を自分で整理し、何とか実装をしようとします。

#### 修正

作成したアプリケーションに変更を加えます。

  **プロンプト例**

  ```text
  作成したダッシュボードに、並び替え機能を追加してください。
  ```

  **プロンプト例**

  ```text
  作成したダッシュボードに、集計機能を追加してください。
  ```

#### 破棄（あとかたづけ）

作成したアプリケーションを破棄し、初期状態に戻します。

  **プロンプト例**

  ```text
  作成したアプリケーションを tmp/ 下に移動して破棄し、初期状態に戻してください。
  ```

#### 番外編

  **プロンプト例**

  ```text
  ブラウザで動くテトリスを作って、ローカルで実行できるようにしてください。
  ```

### たねあかし
- instruction
- skill

### 伝えるポイント

- Vibe Coding は完成品を無検証で受け取ることではなく、対話しながら意図を具体化する入口である。
- エージェントはコード生成だけでなく、探索、編集、実行、検証を連続して行える。
- 人は目的、制約、受け入れ条件を示し、結果を判断する役割を持つ。


## 4. Part 2: 既存リポジトリを起点に AI Native な開発を体験する（95 分）

### 共通シナリオ

事前に選定した既存リポジトリ（レガシーコードに近い構成を含む想定）を、実際の既存プロジェクトとして手元に clone / download しておく。そのリポジトリに対して参加者が新しい機能要求を受けたという設定で進める。同じリポジトリを最後まで使い、次の流れを一つのストーリーとして体験する。

参加者はコードを一から書かず、ローカルのリポジトリを VS Code で開き、用意されたプロンプトを実行して結果を確認する。実装やテスト実行など難易度の高い操作は講師が担当し、各段階で「何を依頼したか」「何が変わったか」「人は何を確認するか」を対話形式で整理する。

題材には、このリポジトリに収録した注文管理システムを使用する。参加者向けの既定データベースはSQLiteとし、Oracle SQL／PL/SQLは解析対象、Oracle Databaseでの実行は講師デモまたは発展演習とする。WebアプリケーションはNode.js標準機能だけで動作し、追加パッケージなしで `npm start` から起動できる。環境構成と両DBの差異は [docs/database-runtime-design.md](docs/database-runtime-design.md) を参照する。

理解 → 文書化 → 変更設計 → 実装 → レビュー → テスト → リスク確認

### 4.1 既存システムを理解する（30 分）

#### 実施内容

- VS Code で clone 済みの対象リポジトリを開き、最初に README とプロジェクトの入口を確認する。
- 「このプロジェクトは何をするものか」をエージェントに説明させる。

  **プロンプト例**

  ```text
  既存システムの分析をします。
  このプロジェクトは何をするものか、初めて見る人向けに説明して。
  ```

- 参加者は用意されたプロンプトを使い、システムの目的と主要機能を確認する。

  **プロンプト例**

  ```text
  既存システムの分析をします。
  このシステムの主要機能を一覧にして。
  ```

- 講師が対象リポジトリのディレクトリ構成、主要コンポーネント、データフロー、外部依存を追加で特定させる。

  **プロンプト例**

  ```text
  既存システムの分析をします。
  ディレクトリ構成と主要コンポーネントの役割を整理して。
  ```

- 主要な処理について、コードではなく業務フローを起点に説明させる。

  **プロンプト例**

  ```text
  既存システムの分析をします。
  注文処理の一連の流れを、コードではなく業務の手順として説明して。
  ```

- 不明点や追加調査が必要な箇所を列挙させる。

  **プロンプト例**

  ```text
  説明だけでは分からない箇所や、追加で調査が必要な箇所を挙げて。
  ```

#### 成果物

- システム概要
- コンポーネント構成図
- 主要ユースケースまたは処理フロー
- 技術的負債・不明点の一覧

### 4.2 ドキュメントと変更設計を作る（30 分）

#### 実施内容

- 用意されたプロンプトで、既存コードから現行仕様を整理させる。

  **プロンプト例**

  ```text
  注文機能の現行仕様を、入力・処理・出力の観点で整理して
  ```

- 参加者が追加機能の要求を一箇所変更し、出力の変化を確認する。

  **プロンプト例**

  ```text
  注文金額が1万円を超えたら送料を無料にする機能を追加して
  ```

- 講師が変更の影響範囲を分析させる。

  **プロンプト例**

  ```text
  この変更が影響する既存の画面と処理を特定して
  ```

- 変更後の設計書、受け入れ条件、実装タスクを作らせる。

  **プロンプト例**

  ```text
  この変更の設計書、受け入れ条件、実装タスクを作成して
  ```

- 画面変更がある場合はワイヤーフレームを作成し、要件と対応づける。

  **プロンプト例**

  ```text
  この変更を反映した画面のワイヤーフレームを作成して
  ```

#### 成果物

- 現行仕様書
- 変更要求と受け入れ条件
- 変更設計書
- ワイヤーフレーム
- 実装タスクリスト

### 4.3 実装、コードレビュー、自動テスト（25 分）

このセクションは講師デモを基本とする。参加者にはコードの記述を求めず、要求、変更結果、レビュー指摘、テスト結果の対応を確認いただく。

#### 実施内容

- 設計書と受け入れ条件をもとに変更を実装させる。

  **プロンプト例**

  ```text
  この設計書と受け入れ条件に沿って変更を実装して
  ```

- 実装差分を別の観点でレビューさせる。

  **プロンプト例**

  ```text
  この差分を、保守性と既存機能への影響の観点でレビューして
  ```

- 正常系、異常系、境界値を含むテストを生成させる。

  **プロンプト例**

  ```text
  この変更に対する正常系、異常系、境界値のテストを作成して
  ```

- テストを実行し、失敗した場合は原因を特定して修正させる。

  **プロンプト例**

  ```text
  テストを実行し、失敗した場合は原因を特定して修正して
  ```

Code Agent（クラウドエージェント）を利用できない環境では、VS Code 内のエージェントを使い、同様の体験を手元のリポジトリで完結させる。

#### 成果物

- 実装差分
- コードレビュー結果
- テストコード
- テスト実行結果

### 4.4 リスクとセキュリティを確認する（10 分）

#### 実施内容

- 変更差分に対して、入力検証、認証・認可、機密情報、依存関係、ログ出力の観点でレビューさせる。

  **プロンプト例**

  ```text
  この差分を、入力検証・認証認可・機密情報・依存関係・ログ出力の観点でレビューして
  ```

- 既存機能への回帰リスクと運用上の影響を整理させる。

  **プロンプト例**

  ```text
  この変更が既存機能に与える回帰リスクと運用上の影響を整理して
  ```

- 指摘ごとに根拠、重要度、修正案を提示させる。

  **プロンプト例**

  ```text
  指摘ごとに根拠、重要度、修正案をセットで提示して
  ```

#### 強調するポイント

- セキュリティ確認は最後だけでなく、要求、設計、実装、レビュー、テストの各段階に組み込む。
- AI の指摘も誤検知や見落としがあるため、人のレビューや既存の検査ツールを置き換えるものではない。
- 実行権限、外部通信、機密データの取り扱いは組織のポリシーに従う。

## 5. AI Native 開発ライフサイクルとしてのまとめ

ワークショップ全体を通じて、GitHub Copilot の使い方を個別機能の紹介ではなく、次の反復サイクルとして体験いただく。

1. 人が目的、制約、受け入れ条件を定義する。
2. エージェントが既存資産を調査し、計画と成果物を作る。
3. エージェントが実装、レビュー、テストを支援する。
4. ツールによる検証と人の判断で品質を確認する。
5. 結果と新たな知見を仕様や開発ルールへ反映する。

## 6. 事前にご確認・ご準備いただきたい事項

### 技術的要素
- 利用可能な GitHub Copilot のモデル
- GitHub Copilot App の利用可否
  - もし使えるならばぜひ紹介する時間を設けたい

### 運用的要素
- 参加者の職種構成（管理職・開発者・非開発者の比率）
- 演習で参加者に残していただきたい成果物のご要望
