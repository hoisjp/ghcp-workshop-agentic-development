# GitHub Copilot ワークショップ アジェンダ

## 1. ワークショップの概要

### 目的

GitHub Copilot を単なるコード補完ツールではなく、既存システムの理解、設計、実装、レビュー、テスト、リスク管理までを支援する開発エージェントとして体験する。

### 所要時間・形式

- 全体: 2.5 時間（150 分）
- 形式: 講師デモを中心に、短いガイド付き体験と対話を組み合わせる

### 想定参加者

- 管理職を含む、GitHub Copilot をこれまで直接利用していない方
- ソフトウェア開発への関与や技術知識には幅がある前提
- コードの詳細な読み書きよりも、GitHub Copilot で何が変わるか、どこを人が判断すべきかに関心がある方

### 持ち帰り事項

1. Agentic Development と従来の AI コード補完の違い
2. GitHub Copilot に既存プロジェクトの概要を説明させる体験
3. 既存コードから設計書やワイヤーフレームが生成される流れ
4. 実装、コードレビュー、テストをエージェントがどのように支援するか
5. AI が生成した成果物をそのまま受け入れず、セキュリティや変更リスクを確認する視点

## 2. 全体アジェンダ

| 時間 | セッション | 形式 | 主な成果物・体験 |
|---:|---|---|---|
| 0:00-0:10 | オープニング | 説明 | 本日のゴール、前提、デモ対象の共有 |
| 0:10-0:40 | Part 1: Agentic Development を体験する | ライブデモ | 自然言語の要求から動く成果物を作る体験 |
| 0:40-0:50 | 振り返りと Part 2 への接続 | 対話 | エージェントに任せる範囲と人が判断する範囲の整理 |
| 0:50-1:20 | Part 2-1: 既存システムを理解する | デモ／ガイド付き体験 | システム概要、構成図、主要処理、依存関係 |
| 1:20-1:50 | Part 2-2: ドキュメントと変更設計を作る | ガイド付き体験 | 現行仕様書、変更設計書、ワイヤーフレーム |
| 1:50-2:15 | Part 2-3: 変更を実装し、レビューとテストを行う | 講師デモ／対話 | 実装差分、レビュー結果、テストコード、テスト結果 |
| 2:15-2:25 | Part 2-4: リスクとセキュリティを確認する | デモ／対話 | リスク一覧、セキュリティ観点、修正案 |
| 2:25-2:30 | クロージング | 振り返り | AI Native 開発ライフサイクルの持ち帰り事項 |

合計: 150 分

## 3. Part 1: Agentic Development を体験する（30 分）

### 狙い

冒頭で「ここまでできるのか」という驚きを作り、GitHub Copilot がアプリケーションを生成し、計画し、実装し、結果を確認する様子を短時間で体験する。

### 体験の流れ

1. GitHub Copilot の画面と、今回利用するチャット操作を簡潔に説明する。
2. 身近で理解しやすい業務要求を自然言語で提示する。

### GitHub Copilot の基本機能
- プロンプト実行
- モデルの選択
- セッション
- パーミッション

### アプリケーションの生成

#### 初期作成

  **プロンプト例**

  あくまで一例です。自身の業務に近い内容に変更して構いません。

  最初に生成するときは、ハイパフォーマンスのモデルを選択してください。例： GPT-5.6 Sol, Claude Opus 5, など

  ```text
  ブラウザで動作するダッシュボードを vibe coding で作成します。
  社員の勤怠状況を週次で表示し、稼働が過多になっている社員をすぐに特定できるようにします。
  社員データは仮の名前で表示して20人ほどにしてください。
  ```

Learn: 指示が曖昧であっても、エージェントが要件を自分で整理し、何とか実装をしようとします。

#### 修正

作成したアプリケーションに変更を加えます。

  **プロンプト例**

  ```text
  作成したダッシュボードに、並び替え機能を追加してください。
  ```

  **プロンプト例**

  ```text
  作成したダッシュボードに、集計機能を追加してください。
  ```

#### 破棄（あとかたづけ）

作成したアプリケーションを破棄し、初期状態に戻します。

  **プロンプト例**

  ```text
  作成したアプリケーションを tmp/ 下に移動して破棄し、初期状態に戻してください。
  ```

#### 番外編

  **プロンプト例**

  ```text
  ブラウザで動くテトリスを作って、ローカルで実行できるようにしてください。
  ```

### たねあかし
- [copilot-instructions](.github/copilot-instructions.md)
- [vibe-coding skill](.github/skills/vibe-coding/SKILL.md)

### 伝えるポイント

- エージェントはコード生成だけでなく、探索、編集、実行、検証を連続して行える。
- 人は目的、制約、受け入れ条件を示し、結果を判断する役割を持つ。
- Vibe Coding は完成品を無検証で受け取ることではなく、対話しながら意図を具体化する入口である。


## 4. Part 2: 既存リポジトリを起点に AI Native な開発を体験する（95 分）

### 共通シナリオ

事前に選定した既存リポジトリ（レガシーコードに近い構成を含む想定）を、実際の既存プロジェクトとして手元に clone / download しておく。そのリポジトリに対して新しい機能要求を受けたという設定で進める。同じリポジトリを最後まで使い、次の流れを一つのストーリーとして体験する。

コードを一から書くのではなく、ローカルのリポジトリを VS Code で開き、用意されたプロンプトを実行して結果を確認する。実装やテスト実行など難易度の高い操作は講師が担当し、各段階で「何を依頼したか」「何が変わったか」「人は何を確認するか」を対話形式で整理する。

題材には、このリポジトリに収録した注文管理システムを使用する。ワークショップ進行の便宜上、既定データベースはSQLiteとし、Oracle SQL／PL/SQLは解析対象、Oracle Databaseでの実行は手順に含まれていない。WebアプリケーションはNode.js標準機能だけで動作し、追加パッケージなしで `npm start` から起動できる。環境構成と両DBの差異は [docs/database-runtime-design.md](docs/database-runtime-design.md) を参照する。

理解 → 文書化 → 変更設計 → 実装 → レビュー → テスト → リスク確認

### 4.1 既存システムを理解する（30 分）

#### 実施内容

- VS Code で対象リポジトリを開き、最初に README とプロジェクトの入口を確認する。
- と言われても、何を確認すればよいのかわからない場合が多いでしょう。エージェントに聞きましょう。
- 「このプロジェクトは何をするものか」をエージェントに説明させる。

  **プロンプト例**

  ```text
  既存システムの分析をします。
  このプロジェクトは何をするものか、初めて見る人向けに説明して。
  ```

- システムの目的と主要機能を確認する。

  **プロンプト例**

  ```text
  既存システムの分析をします。
  このシステムの主要機能を一覧にして。
  ```

- 対象リポジトリのディレクトリ構成、主要コンポーネント、データフロー、外部依存を追加で特定させる。

  **プロンプト例**

  ```text
  既存システムの分析をします。
  ディレクトリ構成と主要コンポーネントの役割を整理して。
  ブラウザーからDBまでの依存関係をMermaidの構成図で示して。
  コードで確認できた外部依存と、資料にだけ記載されたものを区別して。
  ```

- 主要な処理について、コードではなく業務フローを起点に説明させる。

  **プロンプト例**

  ```text
  既存システムの分析をします。
  注文処理の一連の流れを、コードではなく業務の手順として説明して。
  ```

- 不明点や追加調査が必要な箇所を列挙させる。

  **プロンプト例**

  ```text
  ここまでの説明をコードとDDLで確認し、確認済みの事実と未確定の業務判断を分けて。
  各項目に根拠ファイルを付け、不明点には次に確認する場所か質問を添えて。
  現行分析の結果をdocs/system-analysis.mdへ集約して。アプリは変更しないで。
  ```

#### 成果物

- システム概要
- コンポーネント構成図
- 主要ユースケースまたは処理フロー
- 技術的負債・不明点の一覧

### 4.2 ドキュメントと変更設計を作る（30 分）

#### 今回の変更要求

現行アプリには送料がなく、金額表示は日本語でもUSDである。通貨やDBを変えず、注文詳細に「現在のルールによる送料見積り」を追加する。請求額の確定・保存ではなく、すべての注文状態で参照できる見積りとする。

| 項目 | 演習の既定値・制約 |
|---|---|
| 通常送料 | 5.00 USD |
| 送料無料条件 | 商品小計が100.00 USDを**超える**場合。100.00 USDちょうどは有料 |
| 計算元 | 保存済み注文明細から得られる既存の `order_total`。クライアントから金額を受け取らない |
| 計算場所 | アプリケーション側の共通計算処理。DBビューを含むスキーマには手を加えない |
| 表示 | 注文詳細に商品小計、送料見積り、送料込み見積合計、送料無料までの不足額を表示。日本語・英語に対応 |
| 不足額 | 小計をセント単位で扱い、無料条件を満たす最小額100.01 USDとの差。無料なら0 |
| 対象外 | 注文登録画面のリアルタイム計算、請求・決済、税・割引、送料の永続化、既存一覧・集計の変更 |

#### 実施内容

- 既存コードから現行仕様を整理させる。

  **プロンプト例**

  ```text
  注文機能の現行仕様を、入力・処理・出力の観点で整理して。
  金額の通貨、価格の取得元、order_totalの算出箇所、在庫更新とエラー時の動作を確認して。
  docs/system-analysis.mdの該当章を更新し、他の章は保持して。実装はしないで。
  ```

- 送料見積りの要求を整理する。「100 USDを超える」を「100 USD以上」に変え、100 USDちょうどの判定と不足額の違いを比較する。比較後、後続の演習は既定の「超える」に戻す。

  **プロンプト例**

  ```text
  注文詳細に送料見積りを追加する要求を整理して。まだ実装しないで。
  通常送料は5 USD、商品小計が100 USDを超える場合だけ無料とします。
  小計100 USDちょうどでは送料が必要です。通貨は既存どおりUSDです。
  商品小計・送料見積り・送料込み見積合計・送料無料までの不足額を日英で表示します。
  不足額は無料条件を満たす最小額100.01 USDとの差とし、無料なら0です。
  保存済みのorder_totalからアプリ側で算出し、DB・ビュー・seed・既存集計は変更しません。
  全注文状態で現在のルールによる見積りを表示し、確定請求額として保存しません。
  注文登録画面や決済処理は対象外です。
  「100 USD以上」に変えた場合の境界値も比較し、採用案は「100 USDを超える」と明記して。
  ```

- 変更の影響範囲を分析させる。

  **プロンプト例**

  ```text
  この送料見積りが影響する画面と処理を根拠ファイル付きで特定して。実装はしないで。
  注文詳細のデータ取得、共通計算処理、HTML描画、日英翻訳、追加テストを確認して。
  変更するものと保持するものを分け、DB・ビュー・既存集計が変更不要であることを確認して。
  ```

- 変更後の設計書、受け入れ条件、実装タスクを作らせる。

  **プロンプト例**

  ```text
  採用案の設計書、受け入れ条件、実装タスクをdocs/shipping-estimate-design.mdに作成して。
  まだアプリやテストは変更しないで。DB変更は禁止、送料はアプリ側だけで計算します。
  金額はセント単位で計算し、不正な小計（負数・NaN・Infinity等）を拒否する設計にして。
  99.99、100.00、100.01 USDの期待値と、既存注文1001・1003の表示例を含めて。
  過去注文も現在ルールで再計算される制約と、既存機能を保持する条件も明記して。
  ```

- 任意チャレンジ：画面変更がある場合はワイヤーフレームを作成し、要件と対応づける。

  **プロンプト例**

  ```text
  docs/shipping-estimate-design.mdに注文詳細のテキストワイヤーフレームを追記して。
  既存画面の構成を保ち、商品小計・送料見積り・送料込み見積合計・不足額の配置を示して。
  有料と無料の2例を示し、各表示を受け入れ条件に対応づけて。HTMLやアプリはまだ作らないで。
  ```

**設計結果の確認手順**

作成された設計書を、次の順番で確認します。この段階ではまだアプリを変更しません。

1. `docs/shipping-estimate-design.md`を開き、送料無料の条件が「商品小計が100.00 USDを超える場合」となっていることを確認します。「100.00 USD以上」ではないため、100.00 USDちょうどでは送料がかかります。
2. 金額の通貨が既存どおりUSDであり、送料をDBへ保存せず、画面を表示するたびに既存の`order_total`から見積もる設計になっていることを確認します。
3. 設計書に記載された注文#1001と注文#1003の表示例を、次の期待結果と見比べます。

```text
注文 #1001                     注文 #1003
商品小計            $77.00     商品小計           $105.80
送料見積り           $5.00     送料見積り           $0.00
送料込み見積合計    $82.00     送料込み見積合計   $105.80
送料無料まであと    $23.01     送料無料
```

4. 注文#1001には5.00 USDの送料と23.01 USDの不足額が表示され、100.00 USDを超えている注文#1003は送料無料になっていることを確認します。

以上の4点を確認できたら、設計内容について参加者から質問や懸念がないかを確認し、4.3の実装へ進みます。期待結果と異なる場合は、実装へ進む前にプロンプトを追加して設計書を修正します。

#### 成果物

- 現行仕様書
- 変更要求と受け入れ条件
- 変更設計書
- ワイヤーフレーム
- 実装タスクリスト

### 4.3 実装、コードレビュー、自動テスト（25 分）

このセクションは講師デモを基本とする。コードは記述せず、要求、変更結果、レビュー指摘、テスト結果の対応を確認する。

#### 実施内容

- 設計書と受け入れ条件をもとに変更を実装させる。

  **プロンプト例**

  ```text
  docs/shipping-estimate-design.mdの採用案と受け入れ条件に沿って変更を実装して。
  DBスキーマ（ビューを含む）・DDL・seedは変更せず、実行時DDLも追加しないで。
  送料見積りはアプリ側で計算し、既存order_total・集計・DBテストの期待値を保持して。
  既存の日英表示と画面構成を保ち、注文詳細だけに見積り表示を追加して。
  変更前からある作業差分を保持し、変更ファイルと受け入れ条件の対応を説明して。
  ```

- 任意チャレンジ：実装差分を別の観点でレビューさせる。

  **プロンプト例**

  ```text
  今回の送料見積りの差分だけを、保守性と既存機能への影響の観点でレビューして。
  計算の重複、100 USDちょうどの扱い、丸め、日英表示、DB変更の混入を確認して。
  根拠ファイル、重要度、修正案を示して。まだ修正せず、指摘と未確認事項を分けて。
  ```

- 正常系、異常系、境界値を含むテストを生成させる。

  **プロンプト例**

  ```text
  今回の変更に対する正常系・異常系・境界値のテストを既存のnode:testで追加して。
  99.99、100.00、100.01 USDの送料・見積合計・不足額を検証して。
  負数・NaN・Infinityの拒否、注文詳細の日英表示、既存order_totalの保持も検証して。
  テストデータは一時DBまたは計算関数への入力で用意し、共有DB・DDL・seedは変更しないで。
  既存テストの期待値は緩めず、既存テストも残して。
  ```

- テストを実行し、失敗した場合は原因を特定して修正させる。

  **プロンプト例**

  ```text
  npm testを実行し、失敗した場合は今回の変更によるものか切り分けて修正して。
  DB・ビュー・seedや既存DBテストの期待値を変えて通すことは禁止です。
  新規テストと既存テストの結果を分けて報告し、未検証事項を明示して。
  データを保持するnpm run app:startで起動し、ポート使用中なら別ポートを使って。
  注文1001・1003の詳細を日英で確認し、見積り表示を受け入れ条件と照合して。
  ```

**確認表**

| 商品小計 (USD) | 送料見積り (USD) | 見積合計 (USD) | 無料までの不足額 (USD) |
|---:|---:|---:|---:|
| 99.99 | 5.00 | 104.99 | 0.02 |
| 100.00 | 5.00 | 105.00 | 0.01 |
| 100.01 | 0.00 | 100.01 | 0.00 |

既存DBテストは商品小計を検証しているため、今回の設計では期待値の修正は不要。失敗したら「テストを合わせる」のではなく、既存の金額やデータを変えていないか確認する。画面では注文1001の有料表示と1003の無料表示を比較し、「100 USD以上」への条件変更が境界値に与える影響を振り返る。

#### 成果物

- 実装差分
- コードレビュー結果
- テストコード
- テスト実行結果

### 4.4 リスクとセキュリティを確認する（10 分）

#### 実施内容

- 入力検証、認証・認可、機密情報、依存関係、ログ出力の観点でレビューさせる。

  **プロンプト例**

  ```text
  このアプリケーションを、入力検証・認証認可・機密情報・依存関係・ログ出力の観点でレビューして。
  docs/application-requirements.mdのローカル教材という前提を確認し、
  今回混入した不具合、既存の問題、意図的な未実装、本番公開時の必須対策を区別して。
  既存の対策も根拠付きで挙げて。レビューのみ行い修正は不要です。
  ```

- 任意チャレンジ：既存機能への回帰リスクと運用上の影響を整理させる。

  **プロンプト例**

  ```text
  送料見積りの追加による既存機能への回帰リスクと運用上の影響を整理して。
  既存集計・在庫・注文状態・日英表示が保たれるか、テスト結果と未検証事項を区別して。
  見積りを確定請求額と誤認するリスク、ルール変更で過去注文の見積りも変わる制約を含めて。
  ```

- 任意チャレンジ：指摘ごとに根拠、重要度、修正案を提示させる。

  **プロンプト例**

  ```text
  指摘ごとに根拠ファイル、発生条件、重要度、修正案、確認方法をセットで提示して。
  今回対応するものと対象外のものを分け、DB変更が必要な案は本演習では採用しないで。
  ```

#### 強調するポイント

- セキュリティ確認は最後だけでなく、要求、設計、実装、レビュー、テストの各段階に組み込む。
- AI の指摘も誤検知や見落としがあるため、人のレビューや既存の検査ツールを置き換えるものではない。
- 今回は自身のプロンプトで検査を行ったが、通常は既存の検査ツールを利用する。
- 認証・認可、CSRF対策、監査ログの未実装は教材上の既知の制約。ただし安全性が保証されるわけではなく、ローカル利用時の残存リスクと本番公開を妨げる事項を分けて説明する。

## 5. AI Native 開発ライフサイクルとしてのまとめ

ワークショップ全体を通じて、GitHub Copilot の使い方を個別機能の紹介ではなく、次の反復サイクルとして体験する。

1. 人が目的、制約、受け入れ条件を定義する。
2. エージェントが既存資産を調査し、計画と成果物を作る。
3. エージェントが実装、レビュー、テストを支援する。
4. ツールによる検証と人の判断で品質を確認する。
5. 結果と新たな知見を仕様や開発ルールへ反映する。

## 6. 事前の確認・準備事項

### 技術的要素
- 利用可能な GitHub Copilot のモデル
- GitHub Copilot App の利用可否
  - もし使えるならばぜひ紹介する時間を設けたい

### 運用的要素
- 職種構成（管理職・開発者・非開発者の比率）
- 演習後に残す成果物への要望
